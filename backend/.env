# Configurações do Servidor
PORT=3000
NODE_ENV=development

# Configurações de Cache (em segundos)
# Estes valores serão lidos pelo seu arquivo config/cache.ts
CHECK_PERIOD=120
WEATHER_TTL=600
GEOCODING_TTL=3600

# Timeout das requisições (opcional)
API_TIMEOUT=5000